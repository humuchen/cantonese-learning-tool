services:
  - type: web
    name: cantonese-learning
    env: node
    buildCommand: npm install
    startCommand: npm start
    rootDir: jyutoing
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
